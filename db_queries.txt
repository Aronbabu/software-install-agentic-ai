-- Run this in your database
SELECT u.username, r.role_name 
FROM app_users u
LEFT JOIN app_user_roles ur ON u.id = ur.user_id
LEFT JOIN app_roles r ON ur.role_id = r.id
WHERE u.username IN ('aron', 'aronbabu')
ORDER BY u.username;